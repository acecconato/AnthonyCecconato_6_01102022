<nav class="container-fluid border-0 navbar navbar-dark navbar-expand-lg bg-dark p-4">
    <a href="{{ path('app_home') }}" class="navbar-brand">Snowtricks</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
            data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent"
            aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse justify-content-end" id="navbarSupportedContent">
        <ul class="navbar-nav">
            {% if app.user %}
                <li class="nav-item">
                    <a href="{{ path('app_user_profile') }}" class="nav-link">Mon compte</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_trick_create') }}">Ajouter une figure</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('security_logout') }}">Déconnexion</a>
                </li>
            {% else %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('security_register') }}">Inscription</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('security_login') }}">Connexion</a>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>
