<div {{ attributes }} >
    {{ form_start(form, {attr: {autocomplete: 'off'}}) }}

    <div class="input-group mb-4">
        <div class="input-group-prepend">
            <span class="input-group-text rounded-0"><i class="bi bi-key-fill"></i></span>
        </div>
        {{ form_widget(form.plainPassword) }}
        {{ form_errors(form.plainPassword) }}
    </div>

    <div class="d-flex gap-3 flex-column justify-content-center align-items-center">
        <button class="btn btn-primary rounded-0" type="submit">Modifier mon mot de passe</button>
        <a href="{{ path('security_login') }}">Revenir sur la page de connexion</a>
    </div>

    {{ form_end(form) }}
</div>
